print('-=' * 30)
print('CAIXA DO BANCO!'.center(40))
print('-=' * 30)
valor = int(input('Que quantia quer sacar? '))
total = valor
ced = 100
totced = 0

while True:
    if total >= ced:
        total -= ced
        totced += 1
    else:
        if totced > 0:
            print(f'Total de {totced} cedulas de R${ced}')
        if ced == 100:
            ced = 50
        elif ced == 50:
            ced = 20
        elif ced == 20:
            ced = 10
        elif ced == 10:
            ced = 1
        totced = 0
        if total == 0:
            break
